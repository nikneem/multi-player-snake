## 1. Favicon

- [x] 1.1 Replace `<link rel="icon" type="image/x-icon" href="favicon.ico">` with `<link rel="icon" type="image/png" href="favicon.png">` in `src/App/src/index.html`
- [x] 1.2 Add `<link rel="apple-touch-icon" href="favicon.png">` immediately after the favicon link

## 2. Open Graph Tags

- [x] 2.1 Add `<meta property="og:type" content="website">` to `<head>`
- [x] 2.2 Add `<meta property="og:title" content="Multi-Player Snake">` to `<head>`
- [x] 2.3 Add `<meta property="og:description" content="...">` to `<head>` with a short description of the game
- [x] 2.4 Add `<meta property="og:url" content="/">` to `<head>`
- [x] 2.5 Add `<meta property="og:image" content="/link-visual.png">` to `<head>`
- [x] 2.6 Add `<meta property="og:image:alt" content="...">` to `<head>` with a meaningful alt text
- [x] 2.7 Add `<meta property="og:site_name" content="Multi-Player Snake">` to `<head>`

## 3. Twitter / X Card Tags

- [x] 3.1 Add `<meta name="twitter:card" content="summary_large_image">` to `<head>`
- [x] 3.2 Add `<meta name="twitter:title" content="Multi-Player Snake">` to `<head>`
- [x] 3.3 Add `<meta name="twitter:description" content="...">` to `<head>` (same text as OG description)
- [x] 3.4 Add `<meta name="twitter:image" content="/link-visual.png">` to `<head>`
- [x] 3.5 Add `<meta name="twitter:image:alt" content="...">` to `<head>` (same text as OG image alt)

## 4. SEO Meta Tags

- [x] 4.1 Add `<meta name="description" content="...">` to `<head>` (same text as OG description)
- [x] 4.2 Add `<meta name="author" content="nikneem">` to `<head>`
- [x] 4.3 Add `<meta name="theme-color" content="#0f0f23">` to `<head>` using the app's primary brand color
- [x] 4.4 Add `<meta name="robots" content="index, follow">` to `<head>`

## 5. Verification

- [x] 5.1 Verify `index.html` has no duplicate favicon links and the old `favicon.ico` link is gone
- [ ] 5.2 Validate the HTML `<head>` using a social media preview tool (e.g., LinkedIn Post Inspector or OpenGraph.xyz) to confirm the preview image and tags render correctly
