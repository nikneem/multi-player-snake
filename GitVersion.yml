mode: ContinuousDelivery

next-version: 1.0.0

branches:
  main:
    regex: ^main$
    tag: ''
    increment: Patch
  feature:
    regex: ^feature[/-]
    tag: '{BranchName}'
    increment: Inherit
  pull-request:
    regex: ^(pull|pull-requests|pr)[/-]
    tag: pr
    increment: Inherit
